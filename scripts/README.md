# Developer Scripts

The repository currently keeps executable developer workflows in the root `Makefile` so they are visible and composable. This directory is reserved for small, documented automation that cannot reasonably live in a Make target. Scripts must not encode safety decisions or deploy to physical equipment.
