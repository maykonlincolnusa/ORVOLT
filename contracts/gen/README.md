# Generated Bindings

This location is generated from `contracts/proto` and intentionally contains no hand-authored models. Run `make generate` (Buf) before a local Go build. The control-plane Docker build runs the matching Go generation step itself so a clean checkout can be container-built.
